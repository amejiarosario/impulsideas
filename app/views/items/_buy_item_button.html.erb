<%= link_to 'Compra', orders_path(order: { amount: item.price, description: item.title}),
  class: 'btn btn-primary', role: 'button', method: :post,
  disable_with: "Espere..." %>
  <%# { image_tag('loading.gif', size: '16', alt: 'procesando') }" %>
